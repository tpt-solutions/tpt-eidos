---
name: Bug report
about: Report incorrect verification, a rejected valid program, or a crash
labels: bug
---

**eidos version** (run `eidos --version`):

**Command and `.eidos` source that triggered the bug** (minimal reproducer preferred):

```
eidos check my_file.eidos
```

```eidos
// paste the .eidos source here
```

**Expected behavior** (accepted / rejected / specific error message):

**Actual behavior** (paste full output):

**Cargo workspace info** (`cargo --version`, OS):
